<h1> 
  Movies
</h1>

<% @movies.each do |movie| %>
  <%= link_to movie.title, movie_path(movie.id) %><br>
  <%= movie.rank %><br>
  <br>
  <% end %>

  <%= button_to 'Create A New Movie To Rank!', new_movie_path, method: :get %>